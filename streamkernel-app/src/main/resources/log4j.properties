# Quiet legacy Log4j 1.x chatter from transitive dependencies such as the
# AWS SDK bundle used by the Delta/MinIO demo path.
log4j.rootLogger=ERROR, NULL
log4j.appender.NULL=org.apache.log4j.varia.NullAppender
